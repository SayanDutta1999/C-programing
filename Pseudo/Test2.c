#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    char vowels[] = {'A', 'E', 'I', 'O', 'U'};
    char word[20];
    int count = 0;
    fgets(word, sizeof(word), stdin);
    int len = strlen(word);
    printf("%d", strlen);
    // for(int i = 0; i <  5; i++){
    //     for(int j = )
    //     int
    // }

    return 0;
}